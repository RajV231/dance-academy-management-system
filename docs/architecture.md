# System Architecture

The system follows a typical full stack web architecture.

Frontend
HTML, CSS and JavaScript handle the user interface.

Backend
Node.js handles the business logic and REST APIs.

Database
Stores student information and payment records.

PWA Layer
Manifest and service workers enable installable app behavior and offline support.

Flow

User Browser -> Frontend UI -> REST API (Node.js) -> Database
